var $ = document;

function changeFilter(path, key, value) {
  var urlParams = new URLSearchParams(window.location.search)
  key = key.toString()
  value = value.toString()
  if (urlParams.has("page")) urlParams.delete("page")

  if (urlParams.has(key)) {
    if (value != "") {
      urlParams.set(key, value)
    } else {
      urlParams.delete(key)
    }
  } else {
    urlParams.append(key, value)
  }

  window.location.href = path + "?" + urlParams.toString()
}

function goToRootPath() {
  window.location.href = "/"
}

$.addEventListener("DOMContentLoaded", function() {
  // if (location.pathname.indexOf("thank_you") !== -1) setTimeout(goToRootPath, 10_000)
})
